export type ProjectType = {
    name: string;
    description: string;
    mainImage: string;
    tags: string[];
};

export type Post = {
    id: string;
    content: string;
    date: string;
    title: string;
};
